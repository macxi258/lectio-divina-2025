# Build-time env vars (NOT secrets — safe to commit).
# Vite inlines these into the production bundle.

# URL of the Cloudflare Worker proxy that hides the API.Bible secret.
VITE_BIBLE_PROXY_URL=https://bible.lectiodivinaprayer.com
VITE_BIBLE_API_BIBLE_ID=179568874c45066f-01
